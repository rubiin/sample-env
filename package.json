{
  "name": "sample-env",
  "version": "2.0.7",
  "description": "Generate sample file from .env for your apps",
  "author": "Rubin Bhandari @rubiin",
  "bin": {
    "senv": "./bin/run"
  },
  "homepage": "https://github.com/rubiin/sample-env",
  "license": "MIT",
  "main": "dist/index.js",
  "repository": "rubiin/sample-env",
  "files": [
    "/bin",
    "/dist",
    "/npm-shrinkwrap.json",
    "/oclif.manifest.json"
  ],
  "dependencies": {
    "@oclif/core": "^3",
    "@oclif/plugin-help": "^5",
    "@oclif/plugin-plugins": "^3.8.4",
    "eslint-config-prettier": "^9.0.0"
  },
  "devDependencies": {
    "@oclif/prettier-config": "^0.2.1",
    "@oclif/test": "^3",
    "@types/chai": "^4",
    "@types/mocha": "^10.0.2",
    "@types/node": "^20",
    "chai": "^4",
    "eslint": "^8.50.0",
    "eslint-config-oclif": "^5",
    "eslint-config-oclif-typescript": "^2.0.1",
    "mocha": "^10",
    "oclif": "^3.17.2",
    "shx": "^0.3.4",
    "ts-node": "^10.9.1",
    "typescript": "^5"
  },
  "oclif": {
    "bin": "senv",
    "dirname": "senv",
    "commands": "./dist/commands",
    "plugins": [
      "@oclif/plugin-help",
      "@oclif/plugin-plugins"
    ],
    "topicSeparator": " ",
    "topics": {
      "hello": {
        "description": "Say hello to the world and others"
      }
    }
  },
  "scripts": {
    "build": "shx rm -rf dist && tsc -b",
    "lint": "eslint . --ext .ts",
    "postpack": "shx rm -f oclif.manifest.json",
    "posttest": "yarn lint",
    "prepack": "yarn build && oclif manifest && oclif readme",
    "test": "mocha --forbid-only \"test/**/*.test.ts\"",
    "version": "oclif readme && git add README.md"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "bugs": "https://github.com/rubiin/sample-env/issues",
  "keywords": [
    "oclif"
  ],
  "types": "dist/index.d.ts"
}
